# Godot 4+ specific ignores
.godot/

# Godot-specific ignores
.import/
export.cfg
export_presets.cfg

# Ignore Addons
/addons

# Imported translations (automatically generated from CSV files)
*.translation
*.import

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json
# ---- Godot Web export build artifacts in project ROOT (regenerable, don't commit) ----
# หมายเหตุ: build สำหรับ GitHub Pages อยู่ใน /web และต้นฉบับ /docs (สองอันนี้ commit ได้)
/2DPlatformer*
/*.pck
/*.wasm

# ไฟล์วิดีโอใหญ่ (ใหญ่เกินลิมิต GitHub - เก็บนอก git หรือ host ที่อื่น)
*.mp4
